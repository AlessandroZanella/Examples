
Simple example of usage of COM.

The server is a simple dll that exposes 3 interfaces:
In this example is used only the interface COM but is not 
used the registration of the COM libraries.
Moreover, that not used the lirabry ATL or mfc it used only the library
Unknwn.h.

